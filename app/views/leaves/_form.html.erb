<%= form_for @leave, url: employee_leaves_path(params[:employee_id]) do |form| %>
  <% if @leave.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(leave.errors.count, "error") %> prohibited this leave from being saved:</h2>

      <ul>
      <% leave.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :employee_id %>
    <%= form.number_field :employee_id, id: :leave_employee_id %>
  </div>

  <div class="field">
    <%= form.label :leave_from_date %>
    <%= form.date_select :leave_from_date, id: :leave_leave_from_date %>
  </div>

  <div class="field">
    <%= form.label :leave_to_date %>
    <%= form.date_select :leave_to_date, id: :leave_leave_to_date %>
  </div>

  <div class="field">
    <%= form.label :reason %>
    <%= form.text_area :reason, id: :leave_reason %>
  </div>

  <div class="field">
    <%= form.label :no_of_days %>
    <%= form.number_field :no_of_days, id: :leave_no_of_days %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
